<SectionWrapper
			title="INFORMAÇÕES DO CLIENTE"
			icon={<UserRound className="w-4 h-4 min-w-4 min-h-4" />}
			actions={
				userHasEditPermission ? (
					<Button variant="ghost" size="xs" onClick={() => setControlClientModal({ id: client.id, isOpen: true })} className="flex items-center gap-1">
						<Pencil className="w-4 h-4 min-w-4 min-h-4" />
						EDITAR
					</Button>
				) : null
			}
		>
			<div className="flex w-full grow flex-col gap-4">
				<div className="w-full flex flex-col gap-2">
					<h1 className="text-xs leading-none tracking-tight">INFORMAÇÕES GERAIS</h1>
					<div className="w-full flex flex-col gap-1.5">
						<div className="w-full flex items-center gap-1.5">
							<UserRound className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">NOME</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.nome}</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<IdCard className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">CPF/CNPJ</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.cpfCnpj}</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<Cake className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">DATA DE NASCIMENTO</h3>
							<h3 className="text-sm font-semibold tracking-tight">
								{formatDataBirthdayAsLocale(client.dataNascimento, true) || "DATA DE NASCIMENTO NÃO DEFINIDA"}
							</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<FaUserTie className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">PROFISSÃO</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.profissao || "PROFISSÃO NÃO DEFINIDA"}</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<FaRing className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">ESTADO CIVIL</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.estadoCivil || "ESTADO CIVIL NÃO DEFINIDO"}</h3>
						</div>
					</div>
				</div>
				<div className="w-full flex flex-col gap-2">
					<h1 className="text-xs leading-none tracking-tight">NFORMAÇÕES DE CONTATO</h1>
					<div className="w-full flex flex-col gap-1.5">
						<div className="w-full flex items-center gap-1.5">
							<UserRound className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">TELEFONE PRIMÁRIO</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.telefonePrimario}</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<MdEmail className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">EMAIL</h3>
							<h3 className="text-sm font-semibold tracking-tight">{client.email}</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<Contact className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">OUTROS CONTATOS</h3>
							<div className="grow flex items-center gap-2 flex-wrap">
								{client.contatos.map((clientContact) => (
									<HoverCard key={clientContact.id}>
										<HoverCardTrigger>
											<div className="flex w-fit items-center gap-2 rounded-lg bg-primary/20 px-2 py-1 shadow-sm">
												<p className="text-[0.6rem] font-medium tracking-tight text-primary/80">{clientContact.contato.nome}</p>
												<div className="flex items-center gap-1">
													<GitBranch className="w-3.5 h-3.5 min-w-3.5 min-h-3.5" />
													<p className="text-[0.6rem] font-medium text-primary/80">{clientContact.relacionamento}</p>
												</div>
											</div>
										</HoverCardTrigger>
										<HoverCardContent className="w-full flex flex-col gap-3">
											<div className="w-full flex items-center gap-1">
												<Contact className="w-4 h-4 min-w-4 min-h-4" />
												<p className="text-xs font-medium tracking-tight text-primary/80">DADOS DO CONTATO</p>
											</div>
											<div className="flex w-full flex-col gap-2">
												<p className="text-xs font-medium tracking-tight text-primary/80">
													<strong className="font-bold">NOME:</strong> {clientContact.contato.nome}
												</p>
												<p className="text-xs font-medium tracking-tight text-primary/80">
													<strong className="font-bold">TELEFONE:</strong> {clientContact.contato.telefonePrimario}
												</p>
												<p className="text-xs font-medium tracking-tight text-primary/80">
													<strong className="font-bold">CPF/CNPJ:</strong> {clientContact.contato.cpfCnpj || "NÃO DEFINIDO"}
												</p>
												<p className="text-xs font-medium tracking-tight text-primary/80">
													<strong className="font-bold">RELACIONAMENTO:</strong> {clientContact.relacionamento}
												</p>
											</div>
										</HoverCardContent>
									</HoverCard>
								))}
							</div>
						</div>
					</div>
				</div>
				<div className="w-full flex flex-col gap-2">
					<h1 className="text-xs leading-none tracking-tight">INFORMAÇÕES DE LOCALIZAÇÃO</h1>
					<div className="w-full flex flex-col gap-1.5">
						<div className="w-full flex items-center gap-1.5">
							<Building2 className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">CIDADE (ESTADO)</h3>
							<h3 className="text-sm font-semibold tracking-tight">
								{client.localizacaoCidade} ({client.localizacaoUf})
							</h3>
						</div>
						<div className="w-full flex items-center gap-1.5">
							<MdLocationPin className="w-4 h-4" />
							<h3 className="text-sm font-semibold tracking-tighter text-primary/80">LOCALIZAÇÃO</h3>
							<h3 className="text-sm font-semibold tracking-tight">{clientLocation}</h3>
						</div>
					</div>
				</div>
			</div>
			<div className="flex w-full items-center justify-end gap-1">
				<div className="flex items-center gap-1">
					<BsCalendarPlus />
					<p className="text-[0.65rem] font-medium text-primary/80">{formatDateAsLocale(client.dataInsercao, true)}</p>
				</div>
				<div className="flex items-center gap-1">
					<Avatar className="h-5 w-5">
						<AvatarImage src={client.autor.avatar || undefined} alt={client.autor.nome} />
						<AvatarFallback>{formatNameAsInitials(client.autor.nome || "")}</AvatarFallback>
					</Avatar>
					<p className="text-[0.65rem] font-medium text-primary/80">{client.autor.nome}</p>
				</div>
			</div>
			{controlClientModal.isOpen && (
				<ControlClient
					clientId={client.id}
					userId={userId}
					partnerId={partnerId}
					closeModal={() => setControlClientModal({ id: null, isOpen: false })}
					callbacks={callbacks}
				/>
			)}
		</SectionWrapper>