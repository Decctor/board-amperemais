/**
 * Design tokens da marca Recompra para geração de assets estáticos
 * (fundos de reunião, capas, thumbnails, etc).
 *
 * As cores espelham `styles/globals.css` (--color-brand*) e as cores
 * usadas nos logos de `utils/svgs/logos/`. Se a marca mudar lá, mude aqui.
 */
export const BRAND_COLORS = {
	amber: "#ffb900",
	red: "#e7000b",
	blue: "#24549c",
	black: "#000000",
	white: "#ffffff",
} as const;
